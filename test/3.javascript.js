/** @format */

// 测试noSemi
const a = 'a';
const b = 'b';
const c = 'c';

// 测试quoteProps
const s = {
  name: 'pino',
  age: 18,
};

// 测试arrowParens
const fn = (t) => console.log(t);

// 测试trailingComma
const arr = [1, 2, 3, 4];
const obj = {
  a: 1,
  b: 2,
};
function test(a, b, c) {
  console.log(a, b, c);
}
